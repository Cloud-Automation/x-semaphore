{
  "name": "x-semaphore",
  "version": "0.1.0",
  "description": "Access to native linux semaphores (semget, semop).",
  "main": "build/Release/addon.node",
  "dependencies": {
    "node-addon-api": "^1.6.3"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "node-gyp rebuild"
  },
  "author": "Stefan Pöter <stefan.poeter@cloud-automation.de>",
  "license": "MIT",
  "gypfile": true
}
